services:
  db:
    image: postgres:15-alpine
    environment:
      POSTGRES_USER: benngoan
      POSTGRES_PASSWORD: benngoan
      POSTGRES_DB: benngoan
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U benngoan"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  pgdata:
